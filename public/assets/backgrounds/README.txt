Home background illustration
============================

Save the MAKU study-desk illustration here as:

    home.png

The home screen loads it as a full-screen, aspect-preserving background,
and the four navigation labels are positioned over the empty center area.

After adding it, tell Claude and it will read the image dimensions to
pixel-align the nav labels into the illustration's empty "study space".
